# Only for a split deployment where the dashboard is served from a different
# origin than the API. Baked in at build time by `vite build`; not a runtime
# setting. Leave unset for the normal bundled setup (relative /api).
# VITE_API_URL=https://api.example.com
