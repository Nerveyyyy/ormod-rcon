# ── Database ───────────────────────────────────────────────────────────────────
# SQLite — stored in the ./data volume mount
DATABASE_URL="file:/data/ormod-rcon.db"

# ── Auth ───────────────────────────────────────────────────────────────────────
# Secret used by BetterAuth to sign and verify session tokens.
# Generate a strong random value before deploying:
#   openssl rand -hex 32
# The server will refuse to start in production if this is left as the default.
BETTER_AUTH_SECRET=change_this_to_something_very_random_and_long

# ── Game server ────────────────────────────────────────────────────────────────
# Server name passed as -servername to the game binary.
SERVER_NAME=MyOrmodServer

# Filename of the server executable inside GAME_BINARY_PATH.
# Default: ORMODDirective
# Change if your binary has a different name.
GAME_BINARY_NAME=ORMODDirective

# UDP ports the game listens on.
# These must match the values in serversettings.json — Docker uses them to know
# which ports to forward from the host to the container.
GAME_PORT=27015
QUERY_PORT=27016

# Host IP to bind the game ports to.
# Default: 0.0.0.0 (all interfaces — reachable from any IP on the machine).
# Set to a specific IP to bind game traffic to one NIC only, e.g.:
#   GAME_HOST=203.0.113.5    (public interface)
#   GAME_HOST=10.0.0.1       (LAN interface)
GAME_HOST=0.0.0.0

# Name of the Docker container running the game.
# Must match container_name in the compose file (default: ormod-game).
# The dashboard uses this as the default container when a server record has no
# explicit containerName set.
GAME_CONTAINER_NAME=ormod-game

# ── Paths ──────────────────────────────────────────────────────────────────────
# Path on the HOST to the game binary directory.
# Default: ./docker/game-binary  (relative to docker-compose.yml)
# Dedi-box layout: set to ../server
#GAME_BINARY_PATH=../server

# Path on the HOST to the save data / server config directory.
# Leave unset (or empty) to use a Docker-managed named volume (easiest).
# Set to a host path for direct filesystem access to saves and configs.
#   Dedi-box layout: ../configs
#   Single-machine:  ./data/saves
# When using a host path, ensure the directory is writable by UID 1001
# (the dashboard user) for docker-compose.secure.yml, or UID 1000 (steam)
# for the game container.
#SAVES_PATH=../configs

# ── Dashboard / API ───────────────────────────────────────────────────────────
# Host IP to bind the dashboard port to.
# Default: 0.0.0.0 (all interfaces — accessible from any IP on the machine).
# Set to a specific IP to restrict access, e.g.:
#   DASHBOARD_HOST=10.0.0.1   (LAN interface only)
#   DASHBOARD_HOST=127.0.0.1  (localhost only — use this behind a reverse proxy)
DASHBOARD_HOST=0.0.0.0

# Host port the dashboard is exposed on.
DASHBOARD_PORT=3000

# API port (Fastify). In Docker the dashboard container exposes this internally.
# For local dev, the API listens on this port (default: 3001).
PORT=3001

# Allowed origins for CORS. Comma-separated for multiple origins.
CORS_ORIGIN=http://localhost:3000

# Path to pre-built React static files (set by Dockerfile.dashboard).
# Leave empty for local dev (Vite dev server handles the frontend).
#STATIC_PATH=

# Directory for wipe backups.
BACKUP_PATH=./backups

# ── Docker control (optional) ──────────────────────────────────────────────────
# Set to "false" to disable all Docker socket operations (start/stop/logs/exec).
# Useful if the dashboard is running without Docker access (e.g. remote management
# via RCON only). Defaults to "true" when the socket is mounted.
#DOCKER_CONTROL_ENABLED=true

# Docker socket path (default: /var/run/docker.sock).
#DOCKER_SOCKET=/var/run/docker.sock

# TCP Docker API proxy (alternative to Unix socket). Used by docker-compose.secure.yml.
#DOCKER_HOST=tcp://socket-proxy:2375
